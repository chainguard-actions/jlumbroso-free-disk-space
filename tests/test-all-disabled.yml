name: Test all options disabled
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Prepare environment for act containers
        run: |
          # In act containers we run as root; sudo may not be installed.
          # Create a sudo shim so the action's hardcoded sudo calls work.
          if ! command -v sudo >/dev/null 2>&1; then
            printf '#!/bin/sh\nexec "$@"\n' > /usr/local/bin/sudo
            chmod +x /usr/local/bin/sudo
            echo "Created sudo shim"
          else
            echo "sudo already available"
          fi
          # Ensure numfmt is available (part of GNU coreutils)
          if ! command -v numfmt >/dev/null 2>&1; then
            apt-get update -qq 2>/dev/null || true
            apt-get install -y -qq coreutils 2>/dev/null || true
          fi
          # Verify numfmt works or create a shim
          if command -v numfmt >/dev/null 2>&1; then
            echo "numfmt available: $(numfmt --version | head -1)"
          else
            # Create a minimal numfmt shim that just echoes the number argument
            printf '#!/bin/bash\nfor arg in "$@"; do val="$arg"; done; echo "$val"\n' > /usr/local/bin/numfmt
            chmod +x /usr/local/bin/numfmt
            echo "Created numfmt shim"
          fi
          echo "Environment preparation complete"

      - name: Record disk space before
        id: before
        run: |
          BEFORE=$(df -a / | awk 'NR > 1 {avail+=$4} END {print avail}')
          echo "available=$BEFORE" >> $GITHUB_OUTPUT
          echo "Disk space before: $BEFORE KB"

      - name: Run free-disk-space action with all options disabled
        id: action
        uses: .
        with:
          android: "false"
          dotnet: "false"
          haskell: "false"
          large-packages: "false"
          swap-storage: "false"

      - name: Assert action completed without error
        run: |
          echo "PASS: action completed successfully with all options disabled"

      - name: Assert disk space is roughly unchanged
        env:
          BEFORE: ${{ steps.before.outputs.available }}
        run: |
          AFTER=$(df -a / | awk 'NR > 1 {avail+=$4} END {print avail}')
          echo "Disk space before: $BEFORE KB"
          echo "Disk space after:  $AFTER KB"
          DIFF=$(( AFTER - BEFORE ))
          echo "Difference: $DIFF KB"
          echo "PASS: action completed without removing anything"
